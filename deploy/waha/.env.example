# Domain Caddy serves (a DNS A record must point at this machine)
DOMAIN=wa.example.com
# Same value as WAHA_API_KEY on Vercel. Long and random.
WAHA_API_KEY=change-me-long-random
# The shared GoldOak line. Each agency gets its own session named org-<id>.
WAHA_SESSION=goldoak
